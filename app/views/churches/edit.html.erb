<h1>Edit church</h1>
<%= render 'shared/errors', object: @church %>

#added this from shared error partial from user views video

<%= form_for @church do |form| %>
    <%= render partial: 'fields', locals: {f:form} %>

    <%= form.submit 'Update church profile' %>


<% end %>